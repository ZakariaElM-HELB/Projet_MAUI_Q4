<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyApp.View.SelectColumnsView"
             Title="Colonnes à exporter">

    <VerticalStackLayout Padding="20" Spacing="16">
        <Label Text="Colonnes à inclure :" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" />

        <CollectionView ItemsSource="{Binding ColumnNames}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding IsSelected}" />
                        <Label Text="{Binding Name}" VerticalOptions="Center" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>


        <Button Text="Exporter"
                BackgroundColor="#388e3c"
                TextColor="White"
                Command="{Binding ExportCommand}"
                HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>
